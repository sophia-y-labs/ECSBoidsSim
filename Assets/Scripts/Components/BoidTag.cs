using Unity.Entities;

/// <summary>
/// Tag component: marks an entity as a Boid.
/// Empty component — stores no data, used for query filtering only.
/// </summary>
public struct BoidTag : IComponentData
{
}
